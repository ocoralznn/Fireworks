execute \
    if data storage fireworks:definitions settings{show_actionbar:1b} \
    if items entity @s weapon.* minecraft:clock \
    run return 1

execute \
    if data storage fireworks:definitions settings{show_actionbar:2b} \
    run return 1